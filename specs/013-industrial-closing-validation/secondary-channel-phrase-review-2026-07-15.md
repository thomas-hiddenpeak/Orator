# Secondary-Channel Phrase Evidence Record (2026-07-15)

## Governance

Tools only executed frozen evidence contracts, verified numerical and source
integrity, and arranged unjudged contexts. No code, script, formula, query,
notebook, metric, or algorithm assigned correctness, aggregated accuracy,
ranked or selected a candidate or parameter, or issued a product verdict.

## Frozen generation

FR16ZU was generated once on the retained FR16ZT candidate. The generator
enumerated 1195 structurally complete punctuation-phrase contexts. It produced
no timeline write because no context simultaneously met all frozen gates:

- session-registry and robust-gallery TitaNet had the same top-ranked active
  identity with the inherited duration-class margin;
- that identity conflicted with one uniform known baseline identity;
- the mapped raw Sortformer channel sustained the inherited `0.5` activity
  threshold for at least `0.4 s`; and
- the mapped channel was non-top-1 on at least one phrase frame.

The enumeration count and zero-write result are mechanical properties of the
candidate generation. They are not an evaluation of speaker correctness or
product quality. With no changed context, no contextual semantic verdict can
be made for this branch and the retained FR16ZT baseline remains unchanged.

## Frozen artifacts

- Policy: `speaker-v21-secondary-channel-phrase.toml`
- Metadata: `/tmp/orator-spec013/runtime-v21/native-postprocess/secondary-channel-phrase-metadata.json`
- Candidate: `/tmp/orator-spec013/runtime-v21/native-postprocess/candidate-secondary-channel-phrase.json`

The metadata and candidate record source paths and hashes.
